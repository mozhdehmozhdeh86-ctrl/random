# random
# Random Array Generator

This C# program fills an array with **random values** using the `Random` class and its `Next()` method.  
It demonstrates how to generate random numbers and store them inside an array for further processing or display.

---

## 🎯 What the Program Does

- Creates an array with a user-defined size  
- Generates random integers using `Random.Next()`  
- Fills the array with random values  
- Prints the generated array to the console  

---

## 🛠 Technologies Used

- **Programming Language:** C#  
- **Framework:** .NET / .NET Core  
- **Key Methods:** `Random`, `Next()`

---

## 📥 How to Run

1. Clone or download the project  
2. Open the project in **Visual Studio** or your preferred C# IDE  
3. Build and run the program  
4. Enter the number of elements for the array  

---
